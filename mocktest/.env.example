# Copy to .env for local tooling that loads dotenv files. Never commit real keys.
MOCKTEST_API_KEY=
# Optional OpenAI-compatible endpoint used by config/default.yaml.
MOCKTEST_BASE_URL=https://api.example.invalid/v1
