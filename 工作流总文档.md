# VeriLayer 分层开发完整工作流总文档

> 本文档以“当前仓库根目录”为路径基准，根据其中各模块和 `tutor/tutor-app` 的实际内容整理（2026-07-28；实施状态更新至 2026-08-03）。文中路径均为仓库相对路径，不依赖成员本机盘符。本文同时区分目标工作流、既有人工案例和尚待生产化验证的能力。

---

## 1. 工作流总览

### 1.1 解决什么问题

这套工作流回答两个核心问题：

1. **设计期**：一个复杂系统如何通过递归分层，把每层的需求、架构、测试证据做扎实，使得"何时停止拆分"是一个由证据驱动的判定，而不是拍脑袋？
2. **开发期**：当多个叶节点具备实现条件后，如何用 AI 或多人协作低成本开发，并保证子节点逐层回填到父节点时不发生契约不兼容？

一句话原则：

> **Leaf Gate 决定能不能开始写；Vibe Coding 流程决定怎么写才不会在回填时失控。**

### 1.2 端到端流程图

```
┌────────────────────────────── 单个节点的处理循环 ──────────────────────────────┐
│                                                                               │
│                         ┌──► prd-to-architecture ──► architecture.json         │
│  prd-generation ───────┤                                                     │
│  （生成/派生 PRD）      └──► prd-to-gherkin ─────► testcases.json/*.feature   │
│   prd.md/prd.json               （两条分支并行生成）                           │
│                              │                       │                        │
│                              └───────────┬───────────┘                        │
│                                          ▼                                    │
│                              mocktest（架构模拟验证）                          │
│                              mocktest_report.json +                           │
│                              leaf_gate_evidence.json                          │
│                     非 PASS ──► Architecture 修复 ──► 失败用例重验 ──┐        │
│                                          ▲                          │        │
│                                          └──────────────────────────┘        │
│                     仅最新完整 PASS       │                                    │
│                                          ▼                                    │
│                              leaf-gate（分层判定闸门）                         │
│                                          │                                    │
│              ┌───────────────────────────┼───────────────────────┐            │
│              ▼                           ▼                       ▼            │
│     CONTINUE_LAYERING            STOP_LAYERING                ERROR          │
│     （生成 proposed_children，    （进入代码开发）        （打回上游修证据）    │
│      对每个子节点递归本循环）                                                │
└──────────────────────────────────────────────────────────────────────────────┘
                                          │
                                          ▼
┌──────────────────────── vibe coding（完整代码开发工作流） ────────────────────┐
│  执行矩阵批准 → Leaf 任务包 → Leaf 实现 → Leaf 完成确认 →                      │
│  逐层回填 backfill（Replace Mock With Real Child）→ 父节点兼容性检查 →          │
│  递归向上回填至 root → 最终验收                                                │
└───────────────────────────────────────────────────────────────────────────────┘
```

### 1.3 各阶段与目录对应关系

| 阶段 | 目录 | 形态 | 核心入口 |
|---|---|---|---|
| ① PRD 生成/派生 | `prd-generation/` | Skill + Python CLI | `scripts/run_prd_flow.py` |
| ② 架构生成 | `prd-to-architecture-skill/` | Skill/流程规范；生产执行器仍待补齐 | SKILL.md 驱动的 Agent 流程 |
| ③ PRD→测试用例 | `prd-to-gherkin/` | Skill + Node compiler/formatter/validator | `scripts/run_gherkin_flow.mjs` |
| ④ 架构模拟验证 | `mocktest/` | Python 框架 + Codex Skill | `python -m mock_framework` / `run_subagent_skill.py` |
| ⑤ 分层判定 | `leaf-gate/` | Skill + Python CLI | `scripts/run_leaf_gate.py` |
| ⑥ 代码开发与回填 | `vibe coding/` | Python 编排器 + Skill | `vibecode/scripts/vibecode.py` |

### 1.4 贯穿全程的证据与身份机制

工作流所有正式产物共享一个 **canonical envelope**（见 `vibe coding/vibecode/schemas/common-envelope.schema.json`）：

```
schema_version, run_id, project_id, node_id, parent_node_id,
artifact_id, artifact_type, created_at, generator, status,
input_artifacts, requirement_ids
```

其中 `project_id / node_id / parent_node_id` 与 PRD lineage 必须在同一节点的所有产物间一致；各 producer 保留自己的 `run_id`，只有同一 producer 的成对产物（如 Mocktest report/evidence）要求 run 一致。跨阶段绑定使用 `artifact_id + artifact_schema_version + SHA-256`，Leaf Gate v2 不再错误地要求所有阶段共享 run ID。

证据分类贯穿 PRD、Gherkin 两个阶段，只有 `EXPLICIT`（原文直接支持）和 `VALID_DERIVATION`（由证据必然推出）可进入权威产物；`HYPOTHESIS / UNKNOWN / CONFLICT` 只能进报告，绝不静默进入测试或架构。

### 1.5 既有 tutor 案例的复用边界

`tutor/tutor-app` 的范围必须分开统计：22 个设计节点包、16 套 L2 PRD/Architecture/Testcases/Mocktest/Leaf 五件套、17 个实现叶子和 12 个父级 backfill 完成包，另有多轮集成、端到端、发布报告以及真实代码与测试。这证明“分层工件驱动的人工协作流程”已经产出过完整项目，适合作为：

- Artifact Contract 迁移样本；
- Adapter 与 Schema 的 migration regression；
- `CMP-CONFIG-STORE` strict execution/architecture FAIL 的负向校准样本；
- 独立 fresh S1 Coding/Test 正向校准时的工程模式参考；
- 论文中的迁移案例、成功案例和定性分析材料。

但该案例的 `run-manifest.md` 记录的是人工协调而非根编排器 `run-workflow`；16 组上游结构化产物的 generator 为 `structured-input-preparer`；既有 Mocktest `PASS` 不能单独证明 strict component hop、validator judgment 与 audit 完整执行；代码开发也不是由统一的生产 Coding Executor 完成。因此它**不能直接证明**：

- 真实 PRD、Architecture、Gherkin、Mocktest、Leaf 和 Coding 已通过统一 Artifact Contract 自动贯通；
- strict Mocktest 已对全部节点完整执行；
- 根编排器已真实 Derive 子 PRD 并完成递归；
- C0–C5 对照实验已经完成。

此外，Tutor 的代码、测试、预期行为和 Leaf 标签已经公开；16 个 L2 STOP 由显式 terminal/product-owner policy 强制，MOD-03 也含人工叶子决定。因此 Tutor 或其轻微改写任务不能作为正式未见 benchmark，其 STOP 标签不能作为 Leaf accuracy/κ 的无偏 ground truth。

团队分发 Tutor 前必须从原始归档生成清洁副本：排除 `.env`、`data/`、`.git/`、`.worktrees/`、缓存和 `.superdesign/`，完成 secret/path scan 和 SHA-256 manifest。旧机器绝对路径只保留为 provenance，运行时通过 path-rewrite manifest 重定位或 fail-closed。

实施顺序据此调整为：

1. 开工前：生成清洁团队包；Day 1–2 建立 22/16/17/12 迁移清单、current-state/path-rewrite manifest、双环境 preflight、统一合同和 Adapter/Executor 骨架；
2. Day 3 双轨校准：`CMP-CONFIG-STORE` 复现 strict execution complete + architecture FAIL 并验证下游阻断；独立 fresh S1 完成 strict PASS→Leaf STOP→Coding→pytest→repair；
3. Day 4：运行一个全新 root 的递归文档闭环；
4. Day 5：至少两个全新 STOP leaf 完成代码生成与测试；
5. Day 6：完成 fresh multi-leaf 集成后冻结正式实验配置；
6. Day 7–10：正式任务、hidden tests 和 fresh run 与 Tutor 包物理隔离；只有 contamination gate 通过的独立 benchmark 才进入 C0–C5 定量数据。

---

## 2. 阶段①：PRD 生成（prd-generation）

### 2.1 定位

把产品证据编译为唯一 canonical PRD 模型，Root 生成顶层 PRD，Derive 确定性派生一整层子 PRD。机器权威是 `prd.json`（`artifact_schema_version=prd/v3`），`prd.md` 只是由同一模型生成的固定 12 节只读视图。该阶段拥有产品范围、原子需求与稳定 ID、业务行为与可观察通过/失败判定、NFR 测量定义、可追溯性和排除项；**不生成架构、测试用例或 Gherkin**。

旧 `P1`–`P6` 仅是内部采集状态，不再写入公开 JSON；不得同时维护扁平别名或从生成后的 Markdown 反向恢复第二份权威模型。共享 envelope 使用 `schema_version=1.0`，PRD 内容版本单独使用 `artifact_schema_version=prd/v3`；`status=PASS|FAIL|ERROR` 与 `prd_status=draft|approved|complete` 分离。

### 2.2 两种模式

| 输入 | 模式 | 性质 |
|---|---|---|
| 新产品/新功能请求 | **Root** | 引导式采集 → 范围冻结 → oracle 闭包 → canonicalize → 独立审查 → 原子交付 |
| `parent_prd` + 架构包 + `target_module` | **Derive** | 只按显式所有权继承 → canonicalize → 继承门禁 → 原子交付 |

**Root 的真实执行链**：绑定身份与发布范围 → 原子化 `REQ-*`/`NFR-*` → 补齐功能/NFR contract → 运行 schema、引用、唯一性、ledger 与 consumer profile 检查 → 对 canonical semantic hash 做独立审查 → 输出。启发式 SMART/歧义扫描仅是诊断，不能作为业务授权，也不能绕过缺失 oracle。

**Derive 的真实执行链**：拒绝 draft 父 PRD → 架构只提供直接子节点与显式所有权 → 全层分配父需求/contract/metric/exclusion → 跨 owner contract 要求 `acceptance-contract-projections.yaml` → 每个 child 构建同一 canonical 模型 → 临时区确认五件 bundle 齐全后才写目标。依赖、consumer、precondition、文本相似度和既有 child 输出都不是所有权证据；不明确则整层 fail-closed。

**人工门**仅保留：发布范围、缺失业务 oracle/NFR 测量、Derive contract projection、Root 独立审查。当前需求 ID 不得因排序变化而变化，已退休 ID 不得复用。

### 2.3 CLI

```powershell
# 交互式 Root
python prd-generation/scripts/run_prd_flow.py

# 非交互式 Root（需独立审查产物，绑定 input hash）
python prd-generation/scripts/run_prd_flow.py --input root.json --output-dir artifacts/run-1/root `
  --run-id run-1 --project-id demo --node-id root --model <model> --seed 7 `
  --review-artifact review.json

# Derive 单模块
python prd-generation/scripts/run_prd_flow.py --parent-prd parent/prd.md `
  --architecture-package architecture --target-module <ModuleName> --output child/prd.md

# Derive 整层
python prd-generation/scripts/run_prd_flow.py --derive-all --parent-prd parent/prd.md `
  --architecture-package architecture --output-dir artifacts/run-1

# 会话恢复
python prd-generation/scripts/run_prd_flow.py --resume <session.json>

# Canonical 与三个直接消费者 profile
python prd-generation/scripts/validate_prd.py <prd.json> --consumer canonical
python prd-generation/scripts/validate_prd.py <prd.json> --consumer architecture
python prd-generation/scripts/validate_prd.py <prd.json> --consumer gherkin
python prd-generation/scripts/validate_prd.py <prd.json> --consumer leaf
```

退出码：`0` 成功可交接 / `1` 输入错误 / `2` 质量门禁阻塞 / `3` 环境错误 / `4` 运行时错误 / `5` schema 不兼容。

### 2.4 输出产物

成功的 Root/Derive 固定写出：`prd.md`、`prd.json`、`prd_manifest.json`、`validation_report.json`、`execution_log.json`。manifest 同时绑定 Markdown 与 JSON 的 SHA-256。阻塞 Root 写 `prd.draft.md` 和 `blocking_questions.json`，`status=FAIL/prd_status=draft`，禁止交接。

Markdown 顶级章节无条件固定为：Problem Statement；Scope and Non-goals；Current Release Functional Requirements；Current Release Non-functional Requirements；Architecture Input Contract；Success Metrics；Acceptance Contracts；Oracle Coverage Ledger；Future Backlog/Documented Exclusions；Risks/Dependencies/Blocking Questions；Traceability Index；Review Report。无内容时也保留章节和稳定默认值。

### 2.5 交接契约

`schemas/canonical-prd.schema.json` 是字段、类型、枚举与默认结构的机器权威；`scripts/prd_flow/canonical.py` 实现 Schema 无法表达的唯一 ID、引用闭包、ID 不复用、contract 类型、ledger 与 readiness 语义；`references/prd-to-gherkin-handoff-contract.md` 只保留证据/继承语义，不再复制第二套字段 schema。

Architecture、Gherkin、Leaf 三个 profile 必须在 PRD 侧通过。Leaf 当前正式合同要求的 `depth/max_depth/node_history/requirements` 位于 artifact profile 顶层，不进入 PRD 业务 `payload`。根编排器的 `module-result.json` 由 command adapter 生成，不由独立 PRD CLI 冒充。

---

## 3. 阶段②：架构生成（prd-to-architecture-skill）

### 3.1 定位与形态

该阶段已从两套纯 Markdown 规范收敛为一个可执行的 `architecture/v2` compiler。机器权威是 `architecture.json`；其余 Markdown、manifest、validation report 和 execution log 都由同一模型确定性生成。目录实际仍保留历史拼写 `prd-to-artecture-skill/`，团队 canonical 名称仍为 `prd-to-architecture-skill`。

### 3.2 单一合同、双模式权限

| 规则 | `top_level` | `decompose` |
|---|---|---|
| 用途 | 从根级 approved PRD 建立系统架构 | 细化父架构中一个模块或组件 |
| 父架构 | 禁止 | 必需，且只允许一个精确 `target_node_id` |
| 本层拥有 | 系统边界、第一层 `MOD-*` 模块、跨模块契约、系统级状态/技术/部署 | 所选父节点内部的 `CMP-*`/`SUB-*`/`ADP-*`、内部契约和局部决策 |
| 必须保持 | PRD 产品范围和 requirement 语义 | 父责任/排除项/公共契约/状态所有权/技术部署、兄弟边界和祖先不变量 |
| 父级变更 | 不适用 | 写 `parent-change-request.md`，输出 `FAIL/draft` 并停止 |

两种模式不是两套 Schema：envelope、payload 字段集合、ID 规则、五个固定文件和 12 个 Markdown 章节完全相同，差异只由 `architecture_mode`、`authority_scope` 和 `parent_binding` 表达。

### 3.3 固定输出与状态

成功包严格包含：`architecture.json`、`architecture.md`、`architecture-manifest.yaml`、`validation_report.json`、`execution_log.json`。Decompose 父变更阻塞包只允许额外增加 `parent-change-request.md`。

`architecture.md` 固定 12 节：Design Context；Authority and Boundary；Requirement Allocation；Decomposition and Node Registry；State and Data Ownership；Interfaces and Contracts；Runtime Flows；Technology and Deployment；Decisions and Alternatives；Risks, Assumptions, and Open Questions；Traceability and Child Handoff；Review and Human Gate。无内容也不得删节或改名。

只有 `status=PASS`、`architecture_status=approved|complete`、`ready_for_downstream=true`、review approved、无 open question/change request/未决 `decide_now` 的包才能进入下游。

### 3.4 CLI

```powershell
# 顶层系统架构
python prd-to-artecture-skill/scripts/run_architecture_flow.py top-level `
  --prd <root-prd.json> --design <top-level-design.json> --output-dir <architecture-dir>

# 模块/组件继续细分
python prd-to-artecture-skill/scripts/run_architecture_flow.py decompose `
  --prd <current-node-prd.json> --parent-architecture <parent/architecture.json> `
  --target-node-id <exact-id> --design <decompose-design.json> --output-dir <child-dir>

# 按消费者合同验证
python prd-to-artecture-skill/scripts/validate_architecture.py <architecture.json> --consumer mocktest
```

`operation` 独立表示 `new/revise/migrate`；不能用 `decompose` 冒充文件覆盖，也不能用显示名模糊匹配代替稳定 ID。writer 先在同级临时目录完成 Schema、语义、父不可变性、consumer profile、hash 和 inventory 校验，再原子发布。

### 3.5 下游边界

- PRD Derive 直接消费 `architecture.json` 的 `modules` 投影；
- Mocktest v2 直接消费 `architecture/v2` canonical JSON；Top-Level 与 Decompose 进入相同 IR，`architecture_mode` 只描述架构层级；旧 Markdown 仅属于迁移适配器；
- Leaf Gate v2 直接消费 canonical `payload.nodes/contracts/state_ownership` 与顶层 `depth/max_depth/complexity`，子节点建议只能来自 `payload.nodes` 的无损投影；
- Gherkin 是从 PRD 并行生成的分支，不是 Architecture 的直接消费者；
- Vibe Coding 的 `module-result.json` 是 adapter 回执，不能塞进 Architecture core；
- producer-side profile PASS 不等于 Mocktest strict PASS、Leaf 决策或全链 E2E。

---

## 4. 阶段③：PRD → Gherkin（prd-to-gherkin）

### 4.1 定位

把已批准的 canonical PRD v3 编译成唯一 canonical testcase 模型，再机械渲染为固定 Feature。核心信念：

> 场景数量不等于覆盖完整，语法通过不等于语义正确。

### 4.2 四段编译链

| Stage | 输入→输出 | 关键门禁 |
|---|---|---|
| 1. PRD v3 Admission | `prd.json` → ready source | `PASS`、approved/complete、ready flag、零 blocked oracle、零 blocking question、current atomic requirement 全有 ready Acceptance Contract |
| 2. Testcase Compilation | PRD requirements/contracts/evidence → `testcases/v2` | stable TC/SC ID；每 TC 内最小 evidence-backed obligation；不可映射即 `GENERATION_BLOCKED` |
| 3. Feature Projection | `testcases.json` → `feature/v2` | NFC/UTF-8/LF；固定 metadata/tag/Scenario/步骤顺序；1+ Given、恰一 When、1+ Then；不新增业务含义 |
| 4. Structural Validation | JSON + Feature → report/bundle | JSON Schema、字节级重渲染、官方 Gherkin parser、hash/文件 allowlist；只输出 `STRUCTURE_PASS\|FAIL` |

### 4.3 产物

- **`testcases.json`**：唯一机器权威，`artifact_schema_version=testcases/v2`；直接引用 PRD v3 的 requirement、Acceptance Contract 与 evidence，不复制 FACT/IR/ontology/coverage graph。
- **`testcases.feature`**：`feature/v2` 确定性视图；只用英文关键字，固定 `@REQ/@NFR → @TC` 标签顺序；禁用 Background/Outline/Examples/DocString/DataTable。
- **`testcases_manifest.json`**：输入与四个内容产物的精确字节 hash，以及固定五文件 allowlist。
- **`validation_report.json`**：只报告 `STRUCTURE_PASS|STRUCTURE_FAIL`；Mocktest strict 固定为 `NOT_RUN`。
- **`quality_report.md`**：上述机器事实的固定五节只读视图。

### 4.4 校验脚本

```powershell
# 从 canonical PRD v3 原子生成固定五件 bundle
node prd-to-gherkin/scripts/run_gherkin_flow.mjs --prd <prd.json> --out <new-bundle-dir>

# 校验 Schema/Feature 投影/hash/文件集合
node prd-to-gherkin/scripts/validate_bundle.mjs --bundle <bundle-dir>
```

相同 PRD 字节重复运行必须得到相同五件 bundle。Mocktest v2 直接消费 `testcases.json`；若传入 sibling `testcases.feature`，必须存在同目录 JSON 权威源并通过确定性字节重渲染校验，禁止把 Feature 反向解析成第二事实源。

### 4.5 全局禁止

禁止发明阈值/角色/权限/状态/错误码/重试/超时/业务响应；禁止用常识填充 UNKNOWN；禁止把示例列表当封闭全集；禁止静默选择多个合理解释之一。functional 合同固定展开 main/boundary/exception；NFR 只投影 population/window/pass rule/threshold/unit/exclusion。字段不完整或无法无损映射必须 `GENERATION_BLOCKED`。Composition、Scenario Outline 与诊断图均不在 v2 权威主链；`/fast` 不能绕过证据/oracle/结构门禁。

---

## 5. 阶段④：架构模拟验证（mocktest）

### 5.1 定位

在编码实现之前，用 Gherkin 场景驱动的方式验证架构设计文档：

> 当前架构是否能够完整、明确且可审计地支撑这些业务场景？

### 5.2 Canonical v2 单一公共流程

公共入口不再并列暴露旧 Pipeline 和 strict 两套含义不同的流程。`architecture/v2 + testcases/v2` 先通过 versioned adapter 进入唯一 Canonical IR，再由 strict runner 生成证据，最后由单一 publisher 发布固定 bundle：

```
canonical adapter（身份/hash/结构校验，直接读取结构化字段，不解析 Markdown）
  → requirement → runtime_flow → 首步 contract/to_id 的确定性绑定
  → BOUND 才进入执行；AMBIGUOUS/UNBOUND/INVALID 产生 BLOCKED 与固定空产物
  → 逐场景逐 hop 组件模拟（一跳一个独立 subagent，同场景串行、跨场景并行）
  → contract-check（确定性接口兼容检查，无 LLM）
  → 每场景一个独立 validator subagent（五维验证）
  → validate-run-artifacts（strict audit）
  → Canonical Result → 固定五件交付 bundle
```

旧 Markdown/Feature parser、StepMapper、GapDetector、旧 renderer 和旧 Pipeline 仅作为显式 `legacy-markdown/v1` shadow adapter 保留；在等价回归完成前不物理删除，但不得决定 v2 的公共状态和输出。

### 5.3 核心机制

- **一跳一个组件 subagent**：每个 subagent 只扮演一个组件完成一次 hop，输出固定 schema 的 HopResult（output_message/status/side_effects/state_change/self_check/next_hop）；`next_hop.component` 必须是真实组件名。不允许单个 subagent 模拟整条链路。
- **Strict Audit**：校验输入哈希、每场景有 hops 和 validator 结果、hop 组件合法、`subagent_calls.jsonl` 审计链完整、validator 含五维结果。**只有 `strict_audit.json` 的 `status: PASS` 才能声称完成了 strict 组件级验证**；validator 返回 FAIL 是架构问题，strict audit 失败是验证证据本身不完整，两者不等价。
- **确定性绑定门禁**：只接受 `BOUND`（唯一候选）；`AMBIGUOUS`、`UNBOUND`、`INVALID` 均停止执行并保留字段级 provenance。`confidence` 只用于诊断，entry override、近似别名、显示名或 LLM 猜测不能将其提升为 PASS。
- **确定性接口检查**：纯函数无 LLM，检查相邻 hop 字段满足性、未定义下游调用、then 期望组件可达性、状态转换合法性、孤儿组件、契约覆盖缺口。

#### 5.3.1 Mocktest 的架构修复闭环

Mocktest 不是一次性“验证后直接进入 Leaf Gate”的步骤。每次 strict 验证都必须保存 `mocktest_report`、缺陷、风险和修改建议，并按以下规则处理：

```text
execution COMPLETED + validation PASS + audit PASS → overall PASS → Leaf Gate
execution COMPLETED + validation FAIL/WARNING → overall FAIL/WARNING → 仅修改 Architecture 后重跑
绑定不足 → overall BLOCKED；工具/证据/audit 错误 → overall ERROR → 补齐条件后重跑
```

不得通过改写冻结的 Testcases/Feature 来规避架构问题。旧 Architecture、旧报告和每轮修复后的 hash 必须保留；只有最新 `overall=PASS` 才能成为 Leaf Gate 输入。FAIL 是已完成验证发现的架构缺陷；BLOCKED 是尚未形成唯一入口绑定或执行不完整；ERROR 是工具、证据或 audit 问题，三者均不得进入 Leaf/Coding。

### 5.4 输入与输出

**输入**：推荐直接传 `architecture/v2` 与 `testcases/v2`，也可使用 `mocktest-input/v2` 清单。`project_id`、`node_id`、`source_prd_id` 必须一致；Mocktest 自身 `run_id` 与两个上游 producer `run_id` 相互独立。Feature v2 只能作为经字节校验的只读投影视图。

**固定中间产物**：每次运行都生成九件文件，即使 BLOCKED、零 hop 或 ERROR 也写出合法空集合：`run_manifest.json`、`normalized_input.json`、`extraction_report.json`、`execution_plan.json`、`scenario_events.json`、`contract_check.json`、`validator_results.json`、`strict_audit.json`、`execution_log.json`。

**固定正式交付物**（run-scoped）：

- `mocktest_report.json`：`mocktest-report/v2` 唯一机器主结果；
- `mocktest_report.md`：从主结果纯渲染的固定七章节视图；
- `leaf_gate_evidence.json`：Leaf Gate 的 `ALLOW/BLOCK/ERROR` 最小证据；
- `execution_log.json`：规范执行状态和事件；
- `bundle_manifest.json`：四个内容文件 SHA-256 与 bundle hash。

公共 JSON 统一使用 UTF-8、LF、terminal newline、稳定键排序和固定 Schema。状态拆为五个正交维度：`execution_state`、`validation_verdict`、`audit_state`、`publication_state`、`overall`；strict audit PASS 只证明证据完整，不能替代 validation PASS。

Leaf Gate v2 已直接消费 `mocktest-report/v2` 和 `mocktest-leaf-evidence/v2`：以五维 `states`、全量 coverage、当前 Architecture/Testcases 的文件 hash 和 `gate_recommendation` 联合准入。Mocktest 的 run ID 与上游 producer run ID 不要求相同；报告和 Leaf evidence 自身必须使用同一 Mocktest run ID。

### 5.5 CLI 与后端依赖

```powershell
# Canonical v2 公共入口：检查并建立固定 run workspace
python -m mock_framework inspect-input --architecture <architecture.json> --testcases <testcases.json> --output-dir <run-dir> --run-id <mocktest-run-id>

# Strict 流程（--feature 为兼容参数名，v2 推荐传 testcases.json）
python mocktest/.agents/skills/validate-arch/run_subagent_skill.py run-strict --feature <testcases.json> --arch <architecture.json> --output-dir <run-dir>
python mocktest/.agents/skills/validate-arch/run_subagent_skill.py publish-artifacts --run-dir <run-dir> --output-dir <out>

# 从已保存 strict 证据确定性重新发布
python mocktest/scripts/canonicalize_run.py --run-dir <run-dir> --output-dir <out>
```

后端：配置不再内置具体业务组件、延迟目标、第三方 URL 或自动改写。strict 一键运行依赖本机 `codex.exe`（`MOCK_FRAMEWORK_CODEX_BIN` 可覆盖）；主会话 driver 依赖当前会话的 subagent 能力。Architecture、Testcases 和 Feature 均为冻结输入，默认 `auto_modify=false`。

---

## 6. 阶段⑤：分层判定（leaf-gate）

### 6.1 定位与硬边界

对**单个节点**执行两道闸门：先验证“是否有资格做 Leaf 判定”，再判定“是否继续分层”。它是只读 consumer，不修改 Architecture、不改写冻结的 Testcases/Feature、不生成 traceability/risks，也不凭规则轮询制造子模块。

### 6.2 Mocktest → Architecture → 定向重验 → Leaf 闭环

```text
Mocktest PASS + strict audit PASS + publication COMPLETE + full coverage
  └─→ Leaf admission

Mocktest WARNING / FAIL / BLOCKED
  └─→ RETURN_TO_ARCHITECTURE
      └─→ 按 finding 修复 Architecture（Testcases 冻结）
          └─→ 至少重验全部 affected testcase
              └─→ 发布新的完整 Mocktest PASS 报告
                  └─→ Leaf admission

执行 / audit / publication / 证据错误
  └─→ RETURN_TO_VALIDATION → 修复验证证据并重跑
```

首次全量 PASS 使用 `repair_history.mode=FIRST_PASS` 且 `cycles=[]`。发生过架构缺陷则必须使用 `REPAIRED`，每轮保存失败报告 hash、修复前后 Architecture hash、finding、affected testcase、revalidated testcase 和最终报告 hash；最后一轮必须指向当前 Architecture 与当前 Mocktest 报告。全量重跑可以替代定向重验，但必须覆盖原失败集合。

### 6.3 Canonical v2 输入与准入

入口清单固定为 `leaf_gate_input.json`（`leaf-gate-input/v2`），按相对路径和文件 SHA-256 引用 `prd/v3`、`architecture/v2`、`testcases/v2`、`mocktest-report/v2`、`mocktest-leaf-evidence/v2` 五个当前产物。唯一合同注册表为 `leaf-gate/schemas/leaf-gate-run.schema.json`。

准入同时要求：身份与 PRD lineage 一致；Architecture/Testcases 均引用当前 PRD 字节；Mocktest 引用当前 Architecture/Testcases 字节；Mocktest report/evidence 的 run 和 states 一致；五维状态为 `COMPLETED/PASS/PASS/COMPLETE/PASS`；recommendation 为 `ALLOW`；coverage 对全部 testcase 和 requirement 为完整 PASS。任何一项不成立时 `decision.value=null`，不能进入 Coding。

### 6.4 分层判定与路由

| 结果 | 条件 | `next_action.type` |
|---|---|---|
| `CONTINUE_LAYERING` | 存在确定性/合规语义拆分信号，未达深度上限，且 Architecture 已显式声明至少两个直接子节点 | `DECOMPOSE` |
| `STOP_LAYERING` | 已准入且不存在拆分信号 | `VIBECODE` |
| 无判定 | Mocktest 业务不通过 | `RETURN_TO_ARCHITECTURE` |
| 无判定 | 验证执行、audit 或发布证据有误 | `RETURN_TO_VALIDATION` |
| 无判定 | hash、lineage、coverage、修复链或合同无效 | `FIX_INPUT_CONTRACT` |

`proposed_children` 只能逐字段投影 `architecture/v2.payload.nodes` 的 ID、责任、排除项、需求、状态、依赖与契约，不得创造或轮询分配边界。可选 C1–C5 语义复核只能增加拆分信号，不能覆盖准入、hash、coverage、depth 或显式子节点要求。

### 6.5 固定五件输出

```powershell
python leaf-gate/scripts/run_leaf_gate.py <node-dir> --output-dir <output-dir>
```

- `leaf_gate_report.json`：`leaf-gate-report/v2` 唯一机器主结果；
- `leaf_gate_report.md`：同一结果的固定七章节只读视图；
- `next_action.json`：编排器唯一后续路由；
- `execution_log.json`：固定有序事件；
- `bundle_manifest.json`：四个内容文件 hash 与 bundle hash。

所有输出使用 UTF-8、LF、terminal newline、稳定键排序，不包含生成时间。相同输入字节与 policy 必须产生相同输出字节。退出码：`0` 有效分层判定 / `2` 返回上游 / `3` 节点不存在 / `4` 内部错误 / `5` 合同、hash 或 lineage 错误。

### 6.6 遗留边界

旧 Markdown/Feature 自动发现、自动写 traceability/risks、共享 run ID、顶层 `status/defects`、五套并列 schema、生产 annotation template 和自由文本 LLM 判断均已退出 canonical v2。历史产物只能由独立、只读、带版本的迁移工具处理，不得影响 v2 判定。

---

## 7. 阶段⑥：代码开发与逐层回填（vibe coding）

### 7.1 定位与双轨入口

`vibe coding/` 是完整代码开发工作流，playbook 为 `layered-vibecoding-flow.md`，存在两条轨道：

- **根级编排（新）**：`python vibecode/scripts/vibecode.py run-workflow`，从需求/PRD 一路编排到集成。PRD、Architecture、Gherkin、Mocktest、Leaf Gate 可直接回收固定 canonical bundle；Coding、backfill、integration 仍必须配置真实 command executor。全程使用 run-scoped manifest/checkpoint。
- **遗留 post-Leaf-Gate 流程**：针对已完成 Leaf Gate 判定的项目，用 `vibecode/state.json` + append-only `execution-log.jsonl` 驱动。

核心原则：**开发可以分散，契约和回填必须集中**。回填不是复制代码，而是 "Replace Mock With Real Child"。

### 7.2 根级编排架构

`RootWorkflow`（`root_workflow.py`）按模块槽位执行：`MODULES = ("prd", "architecture", "gherkin", "mocktest", "leaf_gate", "coding", "backfill", "integration")`。外部命令可以显式写 `module-result.json`；若未写，编排器只对五个已冻结 producer bundle 做确定性适配并校验状态/manifest/hash。不存在固定 bundle 或回执时直接 fail-closed。

单节点处理（`_process_node`）：

1. PRD 阶段（input 已是 prd 则 passthrough）；
2. 设计阶段：architecture 与 gherkin 可并行，产出 `design-join.json`（input_hash 必须匹配，输出不得逃逸 attempt 目录）；
3. Mocktest 阶段；非 PASS 按 `next_action` 返回 Architecture 修复或 Validation，Architecture 修复后必须产生新 hash，并对 affected testcase 复验；`no_mock` 仅产生显式 `ablation=true` 的合成证据；
4. Leaf Gate 阶段（`no_leaf_gate`/`non_recursive` 消融模式合成 STOP_LAYERING）；
5. `CONTINUE_LAYERING` → 递归处理每个 child → `_backfill`；
6. `STOP_LAYERING` → `execution.admit_coding` 校验证据 → 调用 `coding` 模块 → `record_delivery`；
7. 全部完成后 `_integrate_root` 调 `integration` 模块。

运行模式：`full_recursive` / `non_recursive` / `no_mock` / `no_leaf_gate`（消融用，产物必须标记 `is_ablation=true`、`full_run=false`）；分支模式 `parallel` / `sequential`。

**可恢复性**：`run_manifest.json`（identity_hash、input_hash、config_hash、seed、mode）+ `checkpoint.json`（state_hash、artifact_hashes、log hash）。resume 必须匹配输入/配置/模型/种子/模式/版本，任何篡改 → 退出码 3。dry-run 只生成计划，不作完成证据。

### 7.3 遗留流程状态机

```
INIT → MATRIX_PENDING_APPROVAL → LEAF_TASKS_READY → LEAF_IMPLEMENTATION
  → LEAF_COMPLETION_REVIEW → BACKFILL → PARENT_COMPATIBILITY
  → FINAL_ACCEPTANCE → DONE        (异常 → BLOCKED)
```

关键命令：

```powershell
python vibecode/scripts/vibecode.py doctor                 # 扫描 leaf，报告可编码节点
python vibecode/scripts/vibecode.py generate-matrix        # 生成执行矩阵/集成图/契约索引
python vibecode/scripts/vibecode.py approve --gate matrix --note "..."   # 批准人工门禁
python vibecode/scripts/vibecode.py generate-leaf-tasks    # 生成每个 leaf 的任务包
python vibecode/scripts/vibecode.py next-step              # 当前阶段下一步提示
python vibecode/scripts/vibecode.py verify-stage           # advance 前必须跑
python vibecode/scripts/vibecode.py advance-state          # 推进状态机
python vibecode/scripts/vibecode.py guard-paths <paths...> # 文件所有权守卫
python vibecode/scripts/vibecode.py contract-diff --parent <f> --child <f>  # 契约语义 diff
python vibecode/scripts/vibecode.py collect-reports        # 汇总 final-report.md
python vibecode/scripts/vibecode.py audit-state [--repair] # 审计/修复状态与事件链
python vibecode/scripts/vibecode.py run-workflow --help    # 根级编排入口
```

### 7.4 开发阶段流程（阶段 0–7）

| 阶段 | 操作者 | 动作 | 产物 |
|---|---|---|---|
| 0 启动 | Integration Owner | 扫描 `STOP_LAYERING` 节点，建 leaf 清单/依赖图/执行矩阵 | `execution-matrix.md`、`integration-map.md`、`global-contract-index.md` |
| 1 批准矩阵 | Integration Owner | **人工门禁 matrix**；为每个 leaf 生成任务包 | `vibecode-task.md`、`allowed-context.md`、`forbidden-changes.md`、`contract-checklist.md`、`verification-checklist.md`、`backfill-target.md` |
| 2 Leaf 实现 | Leaf Owner | 只读 allowed-context 内文件；建契约骨架、mock 依赖、契约测试、按 feature 实现 | `completion-report.md`、`contract-diff.md`、`test-results.md`、`child-completion-package.md` |
| 3 Leaf 完成确认 | Leaf Owner/Reviewer | **人工门禁 leaf_completion**：零失败测试、无 breaking mismatch、无未解决高风险 | 通过后提交 Integration Owner |
| 4 第一层回填 | Integration Owner | 契约 diff 无冲突 → 替换 mock child → 父节点 wiring/adapter/route/DI 注册真实 child，sibling 继续用 fake | `backfill-plan.md`、`integration-wiring-report.md`、`backfill-report.md` |
| 5 父节点兼容检查 | Integration Owner | provider/consumer 契约测试、父集成测试、sibling fake 协作测试、feature smoke、回归 smoke | 父节点变 `COMPLETED_CHILD_READY` |
| 6 逐层向上回填 | Integration Owner | 把已完成父节点视为 child completion package，递归至 root | 每轮重复 diff/wiring/check |
| 7 最终验收 | Integration Owner/Reviewer | **人工门禁 final**：root smoke、关键 E2E、跨模块契约测试、事件流、性能 smoke | `vibecode/final-report.md` |

### 7.5 人工门禁（不可自动批准）

`HUMAN_GATES = {"matrix", "leaf_completion", "contract_change", "final"}`：

- **matrix**：执行矩阵批准（阶段 1）；
- **leaf_completion**：Leaf 完成确认（阶段 3）；
- **contract_change**：`contract-diff` 返回 `CONTRACT_CHANGE_REQUIRED` 时（CLI 退出码 10），停手并产出 `contract-change-request.md`，由 Contract Owner 裁决；
- **final**：最终验收（阶段 7）。

根级流程还要求每层 backfill 有真实记录的 Integration Owner 审批（`approver`+`note`），Fixture 审批仅限测试。

### 7.6 文件所有权边界

- **Leaf Owner**：只能读写 `allowed-context.md` 列出的路径（`guard-paths`/`hook-guard` 强制）；不得改父节点 wiring、sibling 内部、共享契约、root-level DTO/event schema。
- **Integration Owner**：只能改父节点集成层文件（wiring、adapter binding、module registry、route/event 注册、test fixtures、父集成测试）；不得改 sibling/leaf 内部、未经批准的共享契约。
- 共享契约必须变更时：**停手，产出 `contract-change-request.md`**。

### 7.7 契约 diff 与回填机制

`contracts.py` 的 `compare_contracts` 做语义级 diff（接口增删、provider/consumer、参数名/类型/必填性、返回类型、数据 schema、错误、前后置条件、超时/重试/幂等/副作用），18 种差异类型映射到五种 outcome：

| Outcome | 含义 | 处理 |
|---|---|---|
| `MATCH` | 完全兼容 | 直接回填 |
| `ADDITIVE_ONLY` | child 多实现，未破坏契约 | 直接回填 |
| `ADAPTER_NEEDED` | 可用 adapter mapping 解决（如参数类型变化、返回类型变化） | 写 adapter 后回填 |
| `LEAF_FIX_REQUIRED` | leaf 未满足父契约 | 修 leaf |
| `CONTRACT_CHANGE_REQUIRED` | 父契约需变更（新增必填参数、前置条件增强、后置条件减弱） | **人工门禁**，走契约变更流程 |

回填批次（`backfill.py`）：`prepare_batch`（冻结 children delivery 证据、parent_baseline_hash、allowed_write_set、protected_paths、planned_changes、快照）→ `record_checks`（**六项强制检查**：contract、provider_compatibility、consumer_compatibility、parent_integration、feature_smoke、regression；contract 检查必须带 semantic diff 且 PASS 要求 breaking_count=0 且 outcome ∈ {MATCH, ADDITIVE_ONLY}）→ `approve_batch`（真实 approver）→ `apply_batch`（校验 baseline hash/canonical_version/actual_changed_paths 与计划一致）→ 子 delivery 变 `INTEGRATED`，父节点生成新 delivery 变 `READY`，可继续向上回填。

### 7.8 Leaf 节点输入要求

`scan_leaves` 已只接受 Leaf Gate v2：校验 `leaf_gate_report.json + next_action.json + bundle_manifest.json`，并从 `leaf_gate_input.json.current_artifacts` 验证五个 canonical producer 文件的当前 hash。旧 `leaf-gate.report.json`、自动生成 `traceability.md/risks.md`、Markdown 契约和状态别名均已删除。只有 `ADMITTED + STOP_LAYERING + VIBECODE` 可生成 Coding task；真实 Coding executor 和人工门禁仍不可省略。

---

## 8. 跨阶段契约衔接一览

| 衔接 | 上游产物 | 下游消费 | 契约载体 |
|---|---|---|---|
| PRD → 架构 | `prd.json`（`prd/v3`、envelope `PASS` 且 lifecycle 为 `approved` 或 `complete`） | Top-Level 或当前节点 Decompose compiler | `canonical-prd.schema.json` + architecture consumer profile |
| PRD → Gherkin | canonical requirements/contracts/evidence + deterministic Markdown source view | Stage 1 解析（FACT 必须引用精确 source_ref） | Gherkin consumer profile；禁止字段 Feature/Scenario/Given/When/Then/testcase |
| 架构 + Gherkin → Mocktest | `architecture/v2` + `testcases/v2`；Feature 仅作经字节校验的视图 | Canonical v2 adapter + strict runner | `mocktest-input/v2`；`mocktest-run.schema.json` |
| Mocktest → Architecture 修复 | `mocktest-report/v2` 的 finding/修改建议 + 失败报告 hash | 仅修 Architecture，冻结 Testcases；对 affected testcase 重验 | `repair_history.cycles[]` |
| Mocktest → Leaf Gate | 最新 `mocktest-report/v2` + `mocktest-leaf-evidence/v2`，五维 PASS、ALLOW、全量覆盖 | Leaf Gate v2 admission | `leaf-gate-input/v2` + 文件 SHA-256 |
| PRD/架构/测试/Mocktest → Leaf Gate | 五个 canonical JSON，身份、lineage、current-byte hash 与修复链一致 | 只读正式判定 | `leaf-gate-run.schema.json` |
| Leaf Gate → 子层递归 | `leaf_gate_report.json`（CONTINUE_LAYERING）+ `next_action.json`（DECOMPOSE） | 编排器按 Architecture 原生 child projection 创建子节点 | `leaf-gate-report/v2` |
| Leaf Gate → 代码开发 | `leaf_gate_report.json`（STOP_LAYERING）+ `next_action.json`（VIBECODE）+ bundle manifest | v2 scanner/admission 生成受限 Coding task；真实 executor 与人工门禁另行配置 | `leaf-gate-report/v2` + `leaf-gate-next-action/v2` + bundle hash |
| 子节点 → 父节点回填 | child completion package（delivery + 契约） | contract-diff → backfill batch | `contract.schema.json`、`contract-diff.schema.json`、`backfill-batch.schema.json` |

模块插槽机制：vibe coding 根编排器并不内置阶段①–⑤的逻辑，而是通过 command adapter 调用外部命令（prd/architecture/gherkin/mocktest/leaf_gate/coding/backfill/integration 八个槽位），以 `module-result.json` 为统一契约回收结果。这意味着每个阶段的具体实现可替换，只要遵守 envelope + module-result 契约。

---

## 9. 贯穿性设计原则

1. **证据忠实**：每个业务语义必须回溯到精确来源或已批准推导；UNKNOWN/CONFLICT/HYPOTHESIS 显式暴露，绝不静默进入权威产物。
2. **闸门只判不修**：prd-generation 的 Oracle 闭包、prd-to-gherkin 的冻结、mocktest 的语义门禁、leaf-gate 的 ERROR，都是"暴露问题打回上游"而非"就地修补"。
3. **判定与验证独立**：生成链、确定性验证、独立语义评审职责分离（独立评审者只见原始 PRD+Feature+验证合同）。
4. **机器可审计**：全程 canonical envelope 身份字段、内容哈希、append-only 事件日志、manifest/checkpoint；覆盖率由图计算，不手工声明。
5. **人工门禁不可绕过**：范围冻结、基线审批、执行矩阵、leaf 完成、契约变更、每层回填、最终发布。
6. **开发分散、契约集中**：Leaf Owner 有严格文件边界；契约变更集中裁决；回填集中执行且六项检查强制。
7. **失败也是数据**：语义门禁阻断、ERROR 判定、负面结果分别统计，不删除不混淆（支撑论文实验的诚实报告）。

---

## 10. 当前实现状态备忘

- 当前实施的权威工作区为 `.coord-worktree/`；根目录 `vibe coding/` 是历史基线。运行、代码和证据的完成状态必须以隔离工作区内的 Git 提交、run report 和 hash 为准。
- Day 1/Day 2 已完成合同、冻结环境和生产骨架的 Gate；production skeleton 的 dry-run 不代表真实模块执行。
- Day 3 已完成两个校准：fresh S1 为 strict PASS → Leaf STOP → 真实 Coding → public pytest 的正向控制，CMP 为 strict execution complete 但 architecture FAIL 且下游阻断的负向控制。S1 首次通过，未发生 repair；repair loop 仍需由独立的可复现初始失败 fixture 验证。
- Day 4 已完成真实 root 的 PRD/Architecture/Gherkin 生成尝试，但最新完整验证在 Mocktest semantic gate FAIL；必须执行“报告驱动的 Architecture 修复 → 重验”闭环。尚未取得 root CONTINUE → child STOP、Leaf、Coding、回填或集成成功证据。
- `prd-to-artecture-skill/`（canonical 名称 `prd-to-architecture-skill`）：已实现 `architecture/v2` 单一合同、Top-Level/Decompose 双模式 compiler、固定 bundle、Schema/语义/消费者校验与父边界指纹；这不改变 Day 4 尚未取得完整生产闭环证据的事实。
- `mocktest/src/mock_framework/pbt/`：源码缺失（仅 `__pycache__`）。
- `mocktest` strict 运行已在 Day 3/Day 4 使用本机 Codex 入口完成校准；任何 future run 仍须区分 execution completeness、semantic PASS/FAIL 与 tool/system ERROR。
- 正式 C0–C5 task specs、`config/experiments/C0.json` 至 `C5.json`、24-run 数据集、统计产物及真实多叶 Integration/Backfill 仍未完成；它们不能被 S1/CMP 校准或 Day 2 dry-run 替代。
