# VeriLayer 分层开发完整工作流总文档

> 本文档以“当前仓库根目录”为路径基准，根据其中各模块和 `tutor/tutor-app` 的实际内容整理（2026-07-28）。文中路径均为仓库相对路径，不依赖成员本机盘符。本文同时区分目标工作流、既有人工案例和尚待生产化验证的能力。

---

## 1. 工作流总览

### 1.1 解决什么问题

这套工作流回答两个核心问题：

1. **设计期**：一个复杂系统如何通过递归分层，把每层的需求、架构、测试证据做扎实，使得"何时停止拆分"是一个由证据驱动的判定，而不是拍脑袋？
2. **开发期**：当多个叶节点具备实现条件后，如何用 AI 或多人协作低成本开发，并保证子节点逐层回填到父节点时不发生契约不兼容？

一句话原则：

> **Leaf Gate 决定能不能开始写；Vibe Coding 流程决定怎么写才不会在回填时失控。**

### 1.2 端到端流程图

```
┌────────────────────────────── 单个节点的处理循环 ──────────────────────────────┐
│                                                                               │
│                         ┌──► prd-to-architecture ──► architecture.json         │
│  prd-generation ───────┤                                                     │
│  （生成/派生 PRD）      └──► prd-to-gherkin ─────► testcases.json/*.feature   │
│   prd.md/prd.json               （两条分支并行生成）                           │
│                              │                       │                        │
│                              └───────────┬───────────┘                        │
│                                          ▼                                    │
│                              mocktest（架构模拟验证）                          │
│                              mocktest_report.json +                           │
│                              leaf_gate_evidence.json                          │
│                                          │                                    │
│                                          ▼                                    │
│                              leaf-gate（分层判定闸门）                         │
│                                          │                                    │
│              ┌───────────────────────────┼───────────────────────┐            │
│              ▼                           ▼                       ▼            │
│     CONTINUE_LAYERING            STOP_LAYERING                ERROR          │
│     （生成 proposed_children，    （进入代码开发）        （打回上游修证据）    │
│      对每个子节点递归本循环）                                                │
└──────────────────────────────────────────────────────────────────────────────┘
                                          │
                                          ▼
┌──────────────────────── vibe coding（完整代码开发工作流） ────────────────────┐
│  执行矩阵批准 → Leaf 任务包 → Leaf 实现 → Leaf 完成确认 →                      │
│  逐层回填 backfill（Replace Mock With Real Child）→ 父节点兼容性检查 →          │
│  递归向上回填至 root → 最终验收                                                │
└───────────────────────────────────────────────────────────────────────────────┘
```

### 1.3 各阶段与目录对应关系

| 阶段 | 目录 | 形态 | 核心入口 |
|---|---|---|---|
| ① PRD 生成/派生 | `prd-generation/` | Skill + Python CLI | `scripts/run_prd_flow.py` |
| ② 架构生成 | `prd-to-architecture-skill/` | Skill/流程规范；生产执行器仍待补齐 | SKILL.md 驱动的 Agent 流程 |
| ③ PRD→测试用例 | `prd-to-gherkin/` | Skill + Node 校验脚本 | `scripts/validate_*.mjs` |
| ④ 架构模拟验证 | `mocktest/` | Python 框架 + Codex Skill | `python -m mock_framework` / `run_subagent_skill.py` |
| ⑤ 分层判定 | `leaf-gate/` | Skill + Python CLI | `scripts/run_leaf_gate.py` |
| ⑥ 代码开发与回填 | `vibe coding/` | Python 编排器 + Skill | `vibecode/scripts/vibecode.py` |

### 1.4 贯穿全程的证据与身份机制

工作流所有正式产物共享一个 **canonical envelope**（见 `vibe coding/vibecode/schemas/common-envelope.schema.json`）：

```
schema_version, run_id, project_id, node_id, parent_node_id,
artifact_id, artifact_type, created_at, generator, status,
input_artifacts, requirement_ids
```

其中 `schema_version / run_id / project_id / node_id` 四个身份字段必须在同一节点的所有产物间**完全一致**。leaf-gate 会强制校验这一致性；不一致即 `ERROR`。模块之间内部 Schema 暂不统一，靠 Adapter 层在边界转换。

证据分类贯穿 PRD、Gherkin 两个阶段，只有 `EXPLICIT`（原文直接支持）和 `VALID_DERIVATION`（由证据必然推出）可进入权威产物；`HYPOTHESIS / UNKNOWN / CONFLICT` 只能进报告，绝不静默进入测试或架构。

### 1.5 既有 tutor 案例的复用边界

`tutor/tutor-app` 的范围必须分开统计：22 个设计节点包、16 套 L2 PRD/Architecture/Testcases/Mocktest/Leaf 五件套、17 个实现叶子和 12 个父级 backfill 完成包，另有多轮集成、端到端、发布报告以及真实代码与测试。这证明“分层工件驱动的人工协作流程”已经产出过完整项目，适合作为：

- Artifact Contract 迁移样本；
- Adapter 与 Schema 的 migration regression；
- `CMP-CONFIG-STORE` strict execution/architecture FAIL 的负向校准样本；
- 独立 fresh S1 Coding/Test 正向校准时的工程模式参考；
- 论文中的迁移案例、成功案例和定性分析材料。

但该案例的 `run-manifest.md` 记录的是人工协调而非根编排器 `run-workflow`；16 组上游结构化产物的 generator 为 `structured-input-preparer`；既有 Mocktest `PASS` 不能单独证明 strict component hop、validator judgment 与 audit 完整执行；代码开发也不是由统一的生产 Coding Executor 完成。因此它**不能直接证明**：

- 真实 PRD、Architecture、Gherkin、Mocktest、Leaf 和 Coding 已通过统一 Artifact Contract 自动贯通；
- strict Mocktest 已对全部节点完整执行；
- 根编排器已真实 Derive 子 PRD 并完成递归；
- C0–C5 对照实验已经完成。

此外，Tutor 的代码、测试、预期行为和 Leaf 标签已经公开；16 个 L2 STOP 由显式 terminal/product-owner policy 强制，MOD-03 也含人工叶子决定。因此 Tutor 或其轻微改写任务不能作为正式未见 benchmark，其 STOP 标签不能作为 Leaf accuracy/κ 的无偏 ground truth。

团队分发 Tutor 前必须从原始归档生成清洁副本：排除 `.env`、`data/`、`.git/`、`.worktrees/`、缓存和 `.superdesign/`，完成 secret/path scan 和 SHA-256 manifest。旧机器绝对路径只保留为 provenance，运行时通过 path-rewrite manifest 重定位或 fail-closed。

实施顺序据此调整为：

1. 开工前：生成清洁团队包；Day 1–2 建立 22/16/17/12 迁移清单、current-state/path-rewrite manifest、双环境 preflight、统一合同和 Adapter/Executor 骨架；
2. Day 3 双轨校准：`CMP-CONFIG-STORE` 复现 strict execution complete + architecture FAIL 并验证下游阻断；独立 fresh S1 完成 strict PASS→Leaf STOP→Coding→pytest→repair；
3. Day 4：运行一个全新 root 的递归文档闭环；
4. Day 5：至少两个全新 STOP leaf 完成代码生成与测试；
5. Day 6：完成 fresh multi-leaf 集成后冻结正式实验配置；
6. Day 7–10：正式任务、hidden tests 和 fresh run 与 Tutor 包物理隔离；只有 contamination gate 通过的独立 benchmark 才进入 C0–C5 定量数据。

---

## 2. 阶段①：PRD 生成（prd-generation）

### 2.1 定位

生成一份 Markdown PRD（Root 模式）或一整层子 PRD（Derive 模式），作为下游的"证据保留型"输入。拥有产品范围、原子需求与稳定 ID、业务行为与可观察通过/失败判定、NFR 测量定义、可追溯性、排除项。**不生成测试用例或 Gherkin**——PRD 中禁止出现 `Feature`、`Scenario`、`Given/When/Then`、`gherkin_count` 或捏造的"基本场景"。

### 2.2 两种模式

| 输入 | 模式 | 性质 |
|---|---|---|
| 新产品/新功能请求 | **Root** | 一次只问一个问题的引导式需求收集；可恢复状态机 |
| `parent_prd` + 架构包 + `target_module` | **Derive** | 确定性全自动；不提问；只继承授权父义务 |

**Root 模式流程（R1–R8）**：

1. **R1 识别产品与发布边界**：每个候选条款标记 `current` / `out_of_version` / `not_applicable`，非 current 必须有 `scope_reason`。
2. **R1A 冻结范围承诺**：MoSCoW 优先级不能替代范围决策。
3. **R2 构建问题模型**。
4. **R3 需求原子化**：稳定 ID（`REQ-###` / `NFR-###`）；当前版本规范性条款必须 `requirement_kind: atomic`。
5. **R4 NFR 测量**：每个当前 NFR 必须有完整 NFR Verification Contract（population、measurement_start/end、unit、threshold、exclusions、pass_rule、evidence_refs 七字段强制）；先区分 `deterministic_invariant` 与 `statistical_nfr`。
6. **R5 功能 Acceptance Contracts**：actor、preconditions、trigger、response、observable_oracles、boundaries（condition+response 对）、exceptions（condition+response 对）、evidence_refs。
7. **R6/R6A Oracle 闭包循环**：构建 Oracle Coverage Ledger（`ready`/`blocked`/`excluded`），反复消除 blocked 直到为 0。
8. **R7 独立 Agent 审查**：由独立通道按 handoff contract 审查；发现问题回到 R6A。
9. **R8 从下游质量报告恢复**：解析下游报告的阻塞项，映射回源需求，只问缺失的授权决策。

**完成标准**：Markdown 产出不等于完成。只有 blocked=0 且独立审查通过，才输出 `status: approved`、`ready_for_test_generation: true` 的正式 `.md`；否则输出 `*.draft.md`，禁止下游交接。

**Derive 模式规则**（要点）：

- 架构仅用于枚举直接子模块和确定所有权；**不能从架构散文或领域知识创造需求**。
- 每个父功能条款/NFR/Contract/成功指标至少分配到一个直接子模块（允许多所有者），并集完整覆盖。
- 继承需求保留完整且类型兼容的父 Acceptance Contract；引用重映射为真实子模块 ID。
- 跨子模块的父 contract 必须有显式架构投影（`acceptance-contract-projections.yaml`，`mode: shared|project`）。
- 失败时不写任何部分子 PRD（原子性）。

### 2.3 CLI

```powershell
# 交互式 Root
python prd-generation/scripts/run_prd_flow.py

# 非交互式 Root（需独立审查产物，绑定 input hash）
python prd-generation/scripts/run_prd_flow.py --input root.json --output-dir artifacts/run-1/root `
  --run-id run-1 --project-id demo --node-id root --model <model> --seed 7 `
  --review-artifact review.json

# Derive 单模块
python prd-generation/scripts/run_prd_flow.py --parent-prd parent/prd.md `
  --architecture-package architecture --target-module <ModuleName> --output child/prd.md

# Derive 整层
python prd-generation/scripts/run_prd_flow.py --derive-all --parent-prd parent/prd.md `
  --architecture-package architecture --output-dir artifacts/run-1

# 会话恢复
python prd-generation/scripts/run_prd_flow.py --resume <session.json>
```

退出码：`0` 成功可交接 / `1` 输入错误 / `2` 质量门禁阻塞 / `3` 环境错误 / `4` 运行时错误 / `5` schema 不兼容。

### 2.4 输出产物

每次运行固定写出：`prd.md`（或 `prd.draft.md`）、`prd.json`（结构化模型）、`prd_manifest.json`（artifact_id、prd_sha256、requirement_ids）、`validation_report.json`、`execution_log.json`；阻塞的 Root 运行额外写 `blocking_questions.json`。

### 2.5 交接契约

`references/prd-to-gherkin-handoff-contract.md` 是规范性下游契约（消费者：prd-to-architecture、prd-to-gherkin）。机械门禁包括：ID 唯一可解析、当前条款原子化、每个当前需求有类型匹配的完整 contract、每个 contract 有证据引用、无冲突响应、coverage ledger 无 blocked、独立审查通过。`downstream-prd-contract.json` 是其机器可读摘要（禁止字段：Feature/Scenario/Given/When/Then/gherkin）。

---

## 3. 阶段②：架构生成（prd-to-architecture-skill）

### 3.1 定位与形态

该目录是**纯 Markdown 流程规范**（无脚本、无 CLI），包含两个互补 Skill：

1. **`architecture-ddd-to-system-design`**：顶层使用。从已认可 PRD 出发，先完成 DDD 战略建模，再映射为系统架构。明确替代"DDD 后直接跑完整 ADD"的流程。
2. **`recursive-architecture-design`**：子层使用。以父层架构包为**绑定契约**，递归细化单个已选节点，产出子层架构包。

### 3.2 顶层流程：DDD → 系统架构（4 阶段）

| 阶段 | 内容 | 关键产物 |
|---|---|---|
| 1. DDD 战略建模 | 7 步：FR 提取→统一语言→事件风暴→限界上下文→聚合建模→上下文映射→一致性回检+领域流程 | `FR.md`、`ubiquitous-language.md`、`domain-events-catalog.md`、`bounded-contexts.md`、`aggregates.md`、`context-map.md`、`domain-flow.md`、`assumptions.md` |
| 2. DDD→架构六类映射 | M1 BC→Module；M2 Aggregate→数据所有权/事务边界；M3 Domain Flow→运行时交互；M4 Domain Event→接口/事件契约；M5 External System→Adapter/ACL；M6 Driver/Constraint→技术/部署 | `architecture-workbench.md` |
| 3. 关键架构取舍确认 | 处理 Key Decision Queue；分类为 `decide_now` / `defer_to_detail_design` / `return_to_ddd` / `implementation_detail`；只有 `decide_now` 生成 2–3 候选并可能暂停等人确认 | 更新 workbench 与受影响 output 文件 |
| 4. 最终输出 | 前置条件：DDD 一致性通过 + 六类映射完成 + 无未决 `decide_now` | `output/` 下 7 文件 |

**最终输出**（严格位于 `docs/architecture/output/`）：`README.md`、`01-system-overview.md`、`02-runtime-architecture.md`、`03-data-and-consistency.md`、`04-interface-contracts.md`、`05-decisions-and-technology.md`、`06-deployment.md`。

接口契约硬性字段：`contract_id`、`contract_type`、Provider/Consumer、Trigger/Protocol、Sync/Async、Schema、`side_effects`、`dependencies`、Error/Timeout/Retry、Idempotency、Versioning、Source FR/Flow/Event。纯查询契约 `side_effects` 必须写 `None; read-only`。

### 3.3 递归子层流程（6 阶段）

| 阶段 | 做什么 | 写到哪里 |
|---|---|---|
| 0. Bind inputs | 解析 mode（new/revise/migrate）、路径、输出安全 | `architecture-manifest.yaml`（draft） |
| 1. Select and lock | 检测父包类型，唯一匹配 `target_node_id`，抽取边界 | manifest + `01-design-context.md` |
| 2. Allocate work | 读当前 PRD，逐条分类需求（inherited/allocated/local/out-of-scope） | `01-design-context.md` |
| 3. Design internals | 细化局部语义，拆子节点，确定状态归属 | `02-architecture-decomposition.md`、`03-state-and-data.md` |
| 4. Realize collaboration | 父流程/契约→内部协作与子节点间契约（C1–C6 映射） | `04-contracts-and-runtime.md` |
| 5. Decide or return | 分类未解决选择；`return_to_parent` 时写 `parent-change-request.md` 并立即停止 | `05-local-decisions.md` |
| 6. Hand off | 检查产物，manifest 状态改 `ready_for_human_gate` | `child-handoff.md` |

**子层产物（7+1 文件）**：`architecture-manifest.yaml`、`01-design-context.md`、`02-architecture-decomposition.md`、`03-state-and-data.md`、`04-contracts-and-runtime.md`、`05-local-decisions.md`、`child-handoff.md`（+ 可选 `parent-change-request.md`）。`child-handoff.md` 暴露精确的 `child_id` 作为下一层调用的 `target_node_id`。

### 3.4 核心约束（防跑偏）

- DDD 阶段只做业务语义，**禁止选择微服务、数据库、消息队列、缓存、部署拓扑或接口技术**。
- **不把 `BC = Module = Microservice` 作为默认规则**；没有业务强一致要求时默认不用分布式事务；数据库产品选择发生在 M2 数据边界之后。
- 每个架构元素必须可追溯：Module→BC；数据所有权→聚合；跨 Module 交互→接口/事件契约；技术组件→PRD/DDD/约束/已批准取舍；部署单元→扩缩容/隔离/合规等约束。
- 递归层父边界规则：不得重画父架构/契约/所有权/兄弟职责；不得重选父技术栈；父定义为内部节点时不得创建服务/容器/公共运行时边界；不得跨父节点转移状态或数据所有权。
- Human Gate：每次交付以人工门禁结束，支持命令 `[APPROVE]`、`[REVISE phase-N]`、`[EXPLAIN decision-id]`、`[PARENT_CHANGE]`、`[NEXT child_id]`。
- **明确不生成代码、测试、脚手架、部署清单**——与 Gherkin/Mocktest 无直接契约，衔接在工作流编排层完成。

---

## 4. 阶段③：PRD → Gherkin（prd-to-gherkin）

### 4.1 定位

建立可审计转换链，把 PRD 转换为可追踪的 Requirement Model、Feature 和 Quality Report。核心信念：

> 场景数量不等于覆盖完整，语法通过不等于语义正确。

### 4.2 八阶段转换链

| Stage | 名称 | 输入→输出 | 关键门禁 |
|---|---|---|---|
| 1 | Evidence-preserving Requirement Parsing | PRD→Requirement Semantic Graph | 每个规范性陈述映射到 FACT 或合法排除；EXPLICIT FACT 的 `introduced_terms` 为空；本阶段无 AC/TC/Scenario |
| 2 | Requirement Semantic Analysis | →Verified Requirement Graph | 每个业务语义字段有证据或显式 UNKNOWN；无静默消歧 |
| 3 | Evidence Validation | →Validated Requirement Baseline | UNKNOWN/HYPOTHESIS/CONFLICT 不得混入可冻结子集；oracle 不完整不进测试推导 |
| 4 | Requirement Freezing | →Frozen Requirement Model | 基线 `FROZEN`、有版本+哈希+审批引用；Decision Register 中已接受决定达到 `BASELINED` |
| 5 | Evidence-constrained Derivation | →Test Obligation Graph | 只允许 10 种已批准推导策略；只有 `VALID_DERIVATION` 进入义务图 |
| 6 | Test Obligation Analysis | →Authoritative Test Conditions | 只有 `AUTHORITATIVE` TC（输入、路径、oracle 均有证据）可权威化；参数化 TC 合同（SCENARIO vs SCENARIO_OUTLINE）强制执行 |
| 7 | Gherkin Scenario Generation | →Feature + Trace Links | 机械渲染，不加业务含义；英文关键字强制；TC 与 Scenario 一一对应 |
| 8 | Independent Semantic Verification | →最终交付 | 确定性验证 + 正向/反向追踪 + 隔离独立评审 + 幻觉检测 |

### 4.3 产物

- **`requirement-model.yaml`**：唯一机器语义来源。保存 Ontology Graph（15 种节点类型：Actor/Action/Object/Data/Condition/Event/State/Constraint/Temporal/Boundary/Policy/Interface/Exception/Metric/Oracle）、Coverage Graph（权威路径 `SOURCE → REQUIREMENT_GROUP → CLAUSE → FACT → REQUIREMENT_IR → TEST_OBLIGATION → TEST_CONDITION → SCENARIO`）及全链追踪。覆盖率必须由图计算，不得手工声明。
- **`[module].feature`**：只渲染冻结 eligible TC；英文 Gherkin 关键字（禁止 `# language: zh-CN` 和中文关键字，但标题与步骤正文可用中文）；每个原子 TC 一个 `@TC-*` Scenario；`VALID_COMPOSITION` 生成独立 `@COMP-*` 组合 Scenario（组合是补充覆盖，不替代原子 TC，不增加覆盖分子）；有阻塞时明确标记为部分产物。
- **`quality-report.md`**：门禁、图计算覆盖率、阻塞、组合、验证和最终状态（`READY_FOR_DELIVERY` / `VALIDATION_FAILED` / `PENDING_FORMAL_APPROVAL` / `BLOCKED_BY_MISSING_REQUIREMENTS` / `NEEDS_INDEPENDENT_REVIEW`）。
- 可选 `requirements-analysis.md`：只读渲染视图，不得成为第二语义来源。

### 4.4 校验脚本

```powershell
# 模型图校验（ontology、pattern candidate、coverage graph、组合）
node prd-to-gherkin/scripts/validate_requirement_graph.mjs <requirement-model.yaml>

# Feature 与模型的双向追踪校验（Gherkin 语法用 @cucumber/gherkin 正式解析器）
node prd-to-gherkin/scripts/validate_feature.mjs <feature> <requirement-model.yaml>
```

`validate_feature.mjs` 检查：基线 FROZEN、TC 恰好渲染一次、render_mode 与 Gherkin 类型一致、Outline 的 Examples 与 `approved_data_rows` 逐行相等、普通 Scenario 步骤与 `step_template` 规范化后字符级一致、标签覆盖全部 root requirement、无 `UNKNOWN`/`HYP-*` 泄漏。

### 4.5 全局禁止

禁止发明阈值/角色/权限/状态/错误码/重试/超时/业务响应；禁止用常识填充 UNKNOWN；禁止把示例列表当封闭全集；禁止静默选择多个合理解释之一；测试技术可以选择输入、边界和行为路径，**不能创造业务响应**；自动修复仅限格式/ID/引用等不改变语义的问题；`/fast` 只能跳过等待交互，不能绕过证据/冻结/oracle/验证门禁。

---

## 5. 阶段④：架构模拟验证（mocktest）

### 5.1 定位

在编码实现之前，用 Gherkin 场景驱动的方式验证架构设计文档：

> 当前架构是否能够完整、明确且可审计地支撑这些业务场景？

### 5.2 两条执行路径

**A. 核心 Python 流程**（`python -m mock_framework run`）：
Loader（Gherkin 解析、Examples 展开、架构文档解析、步骤映射、Gap 检测）→ Simulator（LLM 逐步模拟、延迟计算、状态机）→ Validator（五维判断：structure/flow/state/contract/performance）→ Improvement（PASS / FIX_ARCH / REVIEW / SUPPLEMENT 决策 + 修改建议）。

**B. Strict Subagent 流程**（`run_subagent_skill.py`，正式实验路径）：

```
prepare（组件卡、入口推断、执行计划 plan.json）
  → 语义门禁（入口组件/契约不明确 → semantic_errors.json，属有效验证结果而非程序错误）
  → 逐场景逐 hop 组件模拟（一跳一个独立 subagent，同场景串行、跨场景并行）
  → contract-check（确定性接口兼容检查，无 LLM）
  → 每场景一个独立 validator subagent（五维验证）
  → validate-run-artifacts（strict audit）
  → report + 发布正式交付物
```

### 5.3 核心机制

- **一跳一个组件 subagent**：每个 subagent 只扮演一个组件完成一次 hop，输出固定 schema 的 HopResult（output_message/status/side_effects/state_change/self_check/next_hop）；`next_hop.component` 必须是真实组件名。不允许单个 subagent 模拟整条链路。
- **Strict Audit**：校验输入哈希、每场景有 hops 和 validator 结果、hop 组件合法、`subagent_calls.jsonl` 审计链完整、validator 含五维结果。**只有 `strict_audit.json` 的 `status: PASS` 才能声称完成了 strict 组件级验证**；validator 返回 FAIL 是架构问题，strict audit 失败是验证证据本身不完整，两者不等价。
- **语义门禁**：无法唯一确定入口组件/契约时停止并输出 `semantic_errors.json`——这是有效的架构验证结果，不得伪造组件调用链绕过。
- **确定性接口检查**：纯函数无 LLM，检查相邻 hop 字段满足性、未定义下游调用、then 期望组件可达性、状态转换合法性、孤儿组件、契约覆盖缺口。

### 5.4 输入与输出

**输入**：`mocktest-input/v1` 清单（`schemas/mocktest_input.schema.json`），引用 architecture（markdown_file/markdown_directory/architecture_json）和 testcases（gherkin_file/gherkin_directory/testcases_json）两个 `FormalArtifactRef`，身份字段必须与清单顶层一致。

**正式交付物**（run-scoped）：
- `mocktest_report.json`（`mocktest/v1` 主结果、稳定缺陷 ID、实验指标）
- `mocktest_report.md`（人类摘要）
- **`leaf_gate_evidence.json`**（Leaf Gate 的最小输入：`mocktest_status` PASS/FAIL/ERROR + `gate_recommendation` ALLOW/BLOCK/ERROR + 缺陷数 + 未覆盖需求 + 高风险组件）
- `execution_log.json`（模型、种子、调用/重试/缓存计数、输入输出哈希）

### 5.5 CLI 与后端依赖

```powershell
# Python 框架
python -m mock_framework run --feature features/order.feature --arch arch/order-module.md --output report.md
python -m mock_framework layer-check --parent <parent.md> --children <child1.md> <child2.md>

# Strict 流程
python mocktest/.agents/skills/validate-arch/run_subagent_skill.py prepare --feature <f> --arch <a> --output plan.json
python mocktest/.agents/skills/validate-arch/run_subagent_skill.py run-strict --feature <f> --arch <a> --output-dir <run-dir>
python mocktest/.agents/skills/validate-arch/run_subagent_skill.py publish-artifacts --run-dir <run-dir> --output-dir <out>
```

后端：LLM 通过 `config/default.yaml` 配置（OpenAI 兼容端点或 Anthropic，API key 走 `${MOCKTEST_API_KEY}` 环境变量）；strict 一键运行依赖本机 `codex.exe`（`MOCK_FRAMEWORK_CODEX_BIN` 可覆盖）；canonical driver 模式依赖当前会话的 subagent 能力。

> 注意：`src/mock_framework/pbt/` 当前只有 `__pycache__` 无 `.py` 源文件，PBT 能力处于源码缺失状态。

---

## 6. 阶段⑤：分层判定（leaf-gate）

### 6.1 定位

对**单个节点**做"是否继续拆分"的闸门。**只判定，不修复**：证据不足返回 `ERROR` 让编排器打回上游，绝不默认 `STOP_LAYERING`（否则下游会基于错误证据编码，造成系统性返工）。

### 6.2 三种判定

| 判定 | 语义 | 下游动作（`next_action`） |
|---|---|---|
| `CONTINUE_LAYERING` | 复杂度/风险/边界证据表明再拆一层有实质收益 | `decompose`：编排器按 `proposed_children` 创建子节点，递归整个流程 |
| `STOP_LAYERING` | 责任收敛、接口清晰、需求已验证、架构风险可接受、Mocktest 通过 | `vibecode`：进入代码开发，`proposed_children` 必须为空 |
| `ERROR` | 证据/契约不足以有效判定，或深度耗尽仍需分解 | 打回上游；**不允许进入代码开发** |

### 6.3 正式模式判定逻辑

入口：`python leaf-gate/scripts/run_leaf_gate.py <node-dir> --output <node-dir>/leaf_gate_decision.json`。检测到 `prd.json`/`architecture.json`/`testcases.json`/`mocktest_report.json`（或 `leaf_gate_evidence.json`）任一存在即进入正式模式，要求四件齐全。

判定流水线：

1. 读 `leaf-gate.config.json` 阈值。
2. **身份校验**：四件产物的 `schema_version/run_id/project_id/node_id` 必须一致；`artifact_type` 与文件名对应。
3. **覆盖校验**：`prd.json` 的每个 requirement 都被 `testcases.json` 覆盖，且每个 testcase `status` 为 `PASS`/`COMPLETED`。
4. **Mocktest 校验**：`mocktest_report.status` 必须为 `PASS`，否则 `MOCKTEST_NOT_PASS` → `ERROR`。
5. **阈值比较**：从 architecture/mocktest 提取指标（requirements/components/interfaces/dependencies/architecture_depth/complexity/risks/mock_defects/mock_critical_defects），任一超阈值生成 `triggered_rules`。
6. **决策**：`effective_max_depth = min(prd.max_depth, config.max_recursion_depth)`；
   - 已达深度上限且仍有 triggered_rules → `ERROR`（`MAX_DEPTH_REACHED`）；
   - 有 triggered_rules → `CONTINUE_LAYERING`；
   - 否则 → `STOP_LAYERING`。

`CONTINUE_LAYERING` 时生成 `proposed_children`（child_node_id、name、responsibility、requirement_ids 轮询分配、decomposition_rationale、expected_interfaces、priority），供编排器直接调度。

### 6.4 输出产物（正式模式固定五件）

`leaf_gate_decision.json`（机器消费判定）、`leaf_gate_metrics.json`（指标与触发规则）、`execution_log.json`（含输入/输出哈希）、`leaf_gate_annotation_template.json`（人工标注）、`leaf_gate_decision.md`（人类可读）。

退出码：`0` 成功 / `2` 上游验证未完成 / `3` 配置错误 / `4` 运行时错误 / `5` 契约身份不兼容。

### 6.5 遗留非正式模式

无 JSON 输入时走 legacy 路径：发现 Markdown/Feature 产物 → 自动生成 traceability/risks → C1~C5 静态检查（行为复杂度、契约边界、AI 上下文控制、可验证性、风险分解）→ 可选结合 LLM 语义法官（`--llm-judgement`）。

---

## 7. 阶段⑥：代码开发与逐层回填（vibe coding）

### 7.1 定位与双轨入口

`vibe coding/` 是完整代码开发工作流，playbook 为 `layered-vibecoding-flow.md`，存在两条轨道：

- **根级编排（新）**：`python vibecode/scripts/vibecode.py run-workflow`，从需求/PRD 一路编排到集成，模块全部走 command adapter + `module-result.json` 契约，run-scoped manifest/checkpoint。
- **遗留 post-Leaf-Gate 流程**：针对已完成 Leaf Gate 判定的项目，用 `vibecode/state.json` + append-only `execution-log.jsonl` 驱动。

核心原则：**开发可以分散，契约和回填必须集中**。回填不是复制代码，而是 "Replace Mock With Real Child"。

### 7.2 根级编排架构

`RootWorkflow`（`root_workflow.py`）按模块槽位执行：`MODULES = ("prd", "architecture", "gherkin", "mocktest", "leaf_gate", "coding", "backfill", "integration")`。每个模块是通过 `command_adapter` 配置的外部命令，执行后读取 `output_dir/module-result.json`（schema 强制：`module`、`status` PASS/FAIL/ERROR、`input_hash` SHA-256、`output_artifacts`、`output_hashes`（PASS 时必填）、`duration_ms`、error 字段）。

单节点处理（`_process_node`）：

1. PRD 阶段（input 已是 prd 则 passthrough）；
2. 设计阶段：architecture 与 gherkin 可并行，产出 `design-join.json`（input_hash 必须匹配，输出不得逃逸 attempt 目录）；
3. Mocktest 阶段（`no_mock` 消融模式合成 PASS）；
4. Leaf Gate 阶段（`no_leaf_gate`/`non_recursive` 消融模式合成 STOP_LAYERING）；
5. `CONTINUE_LAYERING` → 递归处理每个 child → `_backfill`；
6. `STOP_LAYERING` → `execution.admit_coding` 校验证据 → 调用 `coding` 模块 → `record_delivery`；
7. 全部完成后 `_integrate_root` 调 `integration` 模块。

运行模式：`full_recursive` / `non_recursive` / `no_mock` / `no_leaf_gate`（消融用，产物必须标记 `is_ablation=true`、`full_run=false`）；分支模式 `parallel` / `sequential`。

**可恢复性**：`run_manifest.json`（identity_hash、input_hash、config_hash、seed、mode）+ `checkpoint.json`（state_hash、artifact_hashes、log hash）。resume 必须匹配输入/配置/模型/种子/模式/版本，任何篡改 → 退出码 3。dry-run 只生成计划，不作完成证据。

### 7.3 遗留流程状态机

```
INIT → MATRIX_PENDING_APPROVAL → LEAF_TASKS_READY → LEAF_IMPLEMENTATION
  → LEAF_COMPLETION_REVIEW → BACKFILL → PARENT_COMPATIBILITY
  → FINAL_ACCEPTANCE → DONE        (异常 → BLOCKED)
```

关键命令：

```powershell
python vibecode/scripts/vibecode.py doctor                 # 扫描 leaf，报告可编码节点
python vibecode/scripts/vibecode.py generate-matrix        # 生成执行矩阵/集成图/契约索引
python vibecode/scripts/vibecode.py approve --gate matrix --note "..."   # 批准人工门禁
python vibecode/scripts/vibecode.py generate-leaf-tasks    # 生成每个 leaf 的任务包
python vibecode/scripts/vibecode.py next-step              # 当前阶段下一步提示
python vibecode/scripts/vibecode.py verify-stage           # advance 前必须跑
python vibecode/scripts/vibecode.py advance-state          # 推进状态机
python vibecode/scripts/vibecode.py guard-paths <paths...> # 文件所有权守卫
python vibecode/scripts/vibecode.py contract-diff --parent <f> --child <f>  # 契约语义 diff
python vibecode/scripts/vibecode.py collect-reports        # 汇总 final-report.md
python vibecode/scripts/vibecode.py audit-state [--repair] # 审计/修复状态与事件链
python vibecode/scripts/vibecode.py run-workflow --help    # 根级编排入口
```

### 7.4 开发阶段流程（阶段 0–7）

| 阶段 | 操作者 | 动作 | 产物 |
|---|---|---|---|
| 0 启动 | Integration Owner | 扫描 `STOP_LAYERING` 节点，建 leaf 清单/依赖图/执行矩阵 | `execution-matrix.md`、`integration-map.md`、`global-contract-index.md` |
| 1 批准矩阵 | Integration Owner | **人工门禁 matrix**；为每个 leaf 生成任务包 | `vibecode-task.md`、`allowed-context.md`、`forbidden-changes.md`、`contract-checklist.md`、`verification-checklist.md`、`backfill-target.md` |
| 2 Leaf 实现 | Leaf Owner | 只读 allowed-context 内文件；建契约骨架、mock 依赖、契约测试、按 feature 实现 | `completion-report.md`、`contract-diff.md`、`test-results.md`、`child-completion-package.md` |
| 3 Leaf 完成确认 | Leaf Owner/Reviewer | **人工门禁 leaf_completion**：零失败测试、无 breaking mismatch、无未解决高风险 | 通过后提交 Integration Owner |
| 4 第一层回填 | Integration Owner | 契约 diff 无冲突 → 替换 mock child → 父节点 wiring/adapter/route/DI 注册真实 child，sibling 继续用 fake | `backfill-plan.md`、`integration-wiring-report.md`、`backfill-report.md` |
| 5 父节点兼容检查 | Integration Owner | provider/consumer 契约测试、父集成测试、sibling fake 协作测试、feature smoke、回归 smoke | 父节点变 `COMPLETED_CHILD_READY` |
| 6 逐层向上回填 | Integration Owner | 把已完成父节点视为 child completion package，递归至 root | 每轮重复 diff/wiring/check |
| 7 最终验收 | Integration Owner/Reviewer | **人工门禁 final**：root smoke、关键 E2E、跨模块契约测试、事件流、性能 smoke | `vibecode/final-report.md` |

### 7.5 人工门禁（不可自动批准）

`HUMAN_GATES = {"matrix", "leaf_completion", "contract_change", "final"}`：

- **matrix**：执行矩阵批准（阶段 1）；
- **leaf_completion**：Leaf 完成确认（阶段 3）；
- **contract_change**：`contract-diff` 返回 `CONTRACT_CHANGE_REQUIRED` 时（CLI 退出码 10），停手并产出 `contract-change-request.md`，由 Contract Owner 裁决；
- **final**：最终验收（阶段 7）。

根级流程还要求每层 backfill 有真实记录的 Integration Owner 审批（`approver`+`note`），Fixture 审批仅限测试。

### 7.6 文件所有权边界

- **Leaf Owner**：只能读写 `allowed-context.md` 列出的路径（`guard-paths`/`hook-guard` 强制）；不得改父节点 wiring、sibling 内部、共享契约、root-level DTO/event schema。
- **Integration Owner**：只能改父节点集成层文件（wiring、adapter binding、module registry、route/event 注册、test fixtures、父集成测试）；不得改 sibling/leaf 内部、未经批准的共享契约。
- 共享契约必须变更时：**停手，产出 `contract-change-request.md`**。

### 7.7 契约 diff 与回填机制

`contracts.py` 的 `compare_contracts` 做语义级 diff（接口增删、provider/consumer、参数名/类型/必填性、返回类型、数据 schema、错误、前后置条件、超时/重试/幂等/副作用），18 种差异类型映射到五种 outcome：

| Outcome | 含义 | 处理 |
|---|---|---|
| `MATCH` | 完全兼容 | 直接回填 |
| `ADDITIVE_ONLY` | child 多实现，未破坏契约 | 直接回填 |
| `ADAPTER_NEEDED` | 可用 adapter mapping 解决（如参数类型变化、返回类型变化） | 写 adapter 后回填 |
| `LEAF_FIX_REQUIRED` | leaf 未满足父契约 | 修 leaf |
| `CONTRACT_CHANGE_REQUIRED` | 父契约需变更（新增必填参数、前置条件增强、后置条件减弱） | **人工门禁**，走契约变更流程 |

回填批次（`backfill.py`）：`prepare_batch`（冻结 children delivery 证据、parent_baseline_hash、allowed_write_set、protected_paths、planned_changes、快照）→ `record_checks`（**六项强制检查**：contract、provider_compatibility、consumer_compatibility、parent_integration、feature_smoke、regression；contract 检查必须带 semantic diff 且 PASS 要求 breaking_count=0 且 outcome ∈ {MATCH, ADDITIVE_ONLY}）→ `approve_batch`（真实 approver）→ `apply_batch`（校验 baseline hash/canonical_version/actual_changed_paths 与计划一致）→ 子 delivery 变 `INTEGRATED`，父节点生成新 delivery 变 `READY`，可继续向上回填。

### 7.8 Leaf 节点输入要求

`scan_leaves` 只接受 decision 归一化为 `STOP_LAYERING` 的节点，且必须具备：`prd.md`、`traceability.md`、`risks.md`、`leaf-gate.report.json`、架构产物目录、接口契约文件（`06-interface-contracts.md` 或 `*contract*.md`）、`*.feature`。缺失即 `INPUT_INCOMPLETE`。`owner-decision.md` 存在时优先级高于普通需求和架构说明。

---

## 8. 跨阶段契约衔接一览

| 衔接 | 上游产物 | 下游消费 | 契约载体 |
|---|---|---|---|
| PRD → 架构 | `prd.md`（status: approved, ready_for_test_generation: true） | DDD 战略建模输入 | `prd-to-gherkin-handoff-contract.md` |
| PRD → Gherkin | `prd.md` 原文 + 稳定行号 | Stage 1 解析（FACT 必须引用精确 source_ref） | 同上；禁止字段 Feature/Scenario/Given/When/Then |
| 架构 + Gherkin → Mocktest | 架构文档/包 + `.feature`（或归一化 JSON） | `mocktest-input/v1` 清单 | `mocktest_input.schema.json` |
| Mocktest → Leaf Gate | `mocktest_report.json` / `leaf_gate_evidence.json`（status 必须 PASS） | 判定依据 + 缺陷指标参与阈值 | `leaf_gate_evidence.schema.json`；`structured_input_contract.json` |
| PRD/架构/测试/Mocktest → Leaf Gate | 四件 JSON，身份字段一致 | 正式判定 | `structured_input_contract.json`（canonical envelope） |
| Leaf Gate → 子层递归 | `leaf_gate_decision.json`（CONTINUE_LAYERING + proposed_children） | 编排器创建子节点，子节点重跑阶段①–⑤ | `leaf-decision.schema.json` |
| Leaf Gate → 代码开发 | `leaf-gate.report.json`（STOP_LAYERING）+ prd/architecture/feature/traceability/risks | `vibecode doctor` / `scan_leaves` / run-workflow coding 槽位 | `module-result.schema.json`；`REQUIRED_LEAF_FILES` |
| 子节点 → 父节点回填 | child completion package（delivery + 契约） | contract-diff → backfill batch | `contract.schema.json`、`contract-diff.schema.json`、`backfill-batch.schema.json` |

模块插槽机制：vibe coding 根编排器并不内置阶段①–⑤的逻辑，而是通过 command adapter 调用外部命令（prd/architecture/gherkin/mocktest/leaf_gate/coding/backfill/integration 八个槽位），以 `module-result.json` 为统一契约回收结果。这意味着每个阶段的具体实现可替换，只要遵守 envelope + module-result 契约。

---

## 9. 贯穿性设计原则

1. **证据忠实**：每个业务语义必须回溯到精确来源或已批准推导；UNKNOWN/CONFLICT/HYPOTHESIS 显式暴露，绝不静默进入权威产物。
2. **闸门只判不修**：prd-generation 的 Oracle 闭包、prd-to-gherkin 的冻结、mocktest 的语义门禁、leaf-gate 的 ERROR，都是"暴露问题打回上游"而非"就地修补"。
3. **判定与验证独立**：生成链、确定性验证、独立语义评审职责分离（独立评审者只见原始 PRD+Feature+验证合同）。
4. **机器可审计**：全程 canonical envelope 身份字段、内容哈希、append-only 事件日志、manifest/checkpoint；覆盖率由图计算，不手工声明。
5. **人工门禁不可绕过**：范围冻结、基线审批、执行矩阵、leaf 完成、契约变更、每层回填、最终发布。
6. **开发分散、契约集中**：Leaf Owner 有严格文件边界；契约变更集中裁决；回填集中执行且六项检查强制。
7. **失败也是数据**：语义门禁阻断、ERROR 判定、负面结果分别统计，不删除不混淆（支撑论文实验的诚实报告）。

---

## 10. 当前实现状态备忘

- `prd-to-architecture-skill/`：架构 Skill/流程规范已存在，但稳定 CLI、结构化 `architecture.json` 和根编排 Adapter 仍待生产化补齐。
- `mocktest/src/mock_framework/pbt/`：源码缺失（仅 `__pycache__`）。
- `mocktest` strict 一键运行依赖本机 `codex.exe`，当前环境不可直接启动（计划中列为预检项）。
- `vibe coding/`：根编排器与遗留流程代码、18 个 JSON schema、测试齐全；`vibecode/state.json` 处于 INIT，`leaves/` 与 `backfill/` 为空，尚无真实运行。
- 真实 Coding/Integration/Backfill Executor 尚未存在（当前为外部模块插槽）——VeriLayer 十天实施计划（`VERILAYER_10_DAY_IMPLEMENTATION_PLAN.md`）的核心交付即补齐这些执行器并跑通 C0–C5 实验。
