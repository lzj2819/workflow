# Forbidden Changes — L05

- 修改 `plugin/` 内其他目录（config/config_store/host/ports 等）与 package.json。
- 修改 contracts/、server/、worker/、shared/、deploy/、docs/。
- 引入模型/网络调用做意图识别（闸门必须确定性）；引入 npm 依赖。
- 实现其他叶子职责；跨边界自行修复问题。
