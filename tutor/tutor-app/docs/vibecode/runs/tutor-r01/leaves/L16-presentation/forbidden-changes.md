# Forbidden Changes — L16

- 修改 `contracts/`、`shared/`、`server/course_app/`（presentation 之外）、既有迁移、兄弟目录、W1/W2 已有实现。
- 隐藏缺失材料标记；快照引入跨模块同步读；实现 ACCESS-GATE/PROJECTOR（backfill）。
- 跨边界自行修复：停止 + contract-change-request 或阻塞说明。
