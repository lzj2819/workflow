# Top-Level Method Note

本目录不再保存独立规范或输出模板。Top-Level 的唯一执行合同位于仓库根 `SKILL.md`、`contracts/architecture-contract-v1.md`、canonical JSON Schema 和 compiler。

DDD 只是一种生成设计草稿的分析方法：识别业务能力、业务不变量、数据所有权、领域事件和外部依赖，然后把结果写入统一的 nodes/state/contracts/flows/decisions 字段。DDD 工作稿不是公开输出，也不能覆盖 PRD 已批准的产品范围。

